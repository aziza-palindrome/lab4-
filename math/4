import math

def area_of_parallelogram(base, height):
    return base * height

base = float(input("base_length: "))
height = float(input("height: "))

area = area_of_parallelogram(base, height)

print(area)